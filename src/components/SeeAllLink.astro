---
import { Link } from './Link';

interface Props {
  title: string;
  link: string;
}

const { title, link } = Astro.props;
---

<div class="text-center py-6">
  <Link to={link} type="button">{title}</Link>
</div>
